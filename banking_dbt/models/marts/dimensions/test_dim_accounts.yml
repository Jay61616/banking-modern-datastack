version: 2

models:
  - name: dim_accounts
    description: "Dimension table for accounts with snapshots"
    columns:
      - name: account_id
        tests:
          - not_null
          - unique

      - name: balance
        tests:
          - not_null

